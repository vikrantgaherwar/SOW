/*
 *   All Routes for SOW must be enclosed by this component
 */
const SowRoutePageWrapper = ({ children }) => (
  <div className="sow-routes-container">{children}</div>
);

export default SowRoutePageWrapper;
